# Fish config (managed by chezmoi private_dot_config)
# Add npm global install directory to PATH (sudo-free global installs)
set -x PATH $HOME/.npm-global/bin $PATH
# Prefer Lerd's PHP, Composer, and Node shims.
set -x PATH $HOME/.local/share/lerd/bin $PATH
# Add Bun install directory to PATH
set -x BUN_INSTALL $HOME/.bun
set -x PATH $BUN_INSTALL/bin $PATH

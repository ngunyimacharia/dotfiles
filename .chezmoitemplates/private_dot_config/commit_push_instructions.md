Commit the current changes using conventional commit format and then push to the remote repository. Never attribute model or {{ .tool }}. Never use emojis, never be friendly. Always use separate commits if makes sense. Analyze the changes and create appropriate commit messages following the conventional commit format: feat, fix, docs, style, refactor, test, chore. Group related changes by component and create separate commits for different types of changes. After committing, push the changes to the remote.

Do not guess when the repo defines the commands explicitly. Prefer the exact project commands from scripts, composer scripts, make targets, task runners, or workflow files. If a required check fails, stop, report the failure, and do not commit or push until all required checks pass.
